# 현재작업

작성일: 2026-05-23
작성 주체: 이안(Yian OS)
대상 프로젝트: mabimobi

## 먼저 읽을 작업 지시서

현재 우선 작업은 아래 파일이야.

```txt
docs/작업지시/v1.2.1_Firestore_기본카드_배포전점검.md
```

## 최근 이안 직접 수정 이력

### 2026-05-23 전체 탭 섹션 라벨 숨김

- 수정 파일: `src/app/page.tsx`
- 변경 내용:
  - `전체` 탭에서 카드 리스트 위에 표시되던 `일일 숙제`, `주간 숙제`, `구매`, `물물교환`, `임무게시판` 섹션 라벨 제거
- 검증:
  - `npm run build` 성공
- 상세 기록:
  - `docs/변경로그.md` 참고

## 현재 배포 전 주의사항

- Firestore 기본 카드 소비 로직 변경분이 아직 실섭 배포 전 상태야.
- 전체 탭 섹션 라벨 숨김 변경도 아직 실섭 배포 전 상태야.
- 배포 전에 `docs/작업지시/v1.2.1_Firestore_기본카드_배포전점검.md` 기준으로 점검 결과를 먼저 요약해야 해.
- 실섭 배포는 내가 명시적으로 요청할 때만 진행해줘.

## 프로젝트 채팅 요청 문구

모비모비 채팅에서는 아래처럼 요청하면 돼.

```txt
docs/작업지시/현재작업.md 파일을 먼저 읽고, 연결된 작업 지시서 기준으로 작업 진행해줘.
```

